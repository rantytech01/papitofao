// Placeholder types. Once the schema is deployed, generate the real
// thing with:
//   npx supabase gen types typescript --project-id YOUR_PROJECT_ID > lib/database.types.ts
// Using `any` here keeps the app compiling in the meantime without
// hiding real type errors once you swap this file in.
export type Database = any;
